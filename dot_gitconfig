[user]
        email = 8335996+double-em@users.noreply.github.com
        name = double-em
[alias]
        co = checkout
        br = branch
        ci = commit
        st = status
        a = add
        all = commit -am
        show-tree = log --oneline --decorate --simplify-by-decoration --graph -all
        lg = log -graph

[credential]
    helper = store

[push]
    default = current

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
